import configureExpressApp from './express';

export default function configureApp() {
    // Otras configuraciones de la aplicación
    // Configuración de la base de datos, variables de entorno, etc.

    return configureExpressApp();
}